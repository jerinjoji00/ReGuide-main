# Frontend runtime environment variables
# Point this at the deployed Flask backend or leave it as localhost during development.
VITE_BACKEND_URL=http://localhost:5000

# Razorpay key id is safe to expose to the browser; keep the secret only in backend/.env
VITE_RAZORPAY_KEY_ID=rzp_test_SYZQ0iOturG076